# `server/docs/` — backend documentation

Stable explanations for the API package. The module map, the request/DI flow and the API
surface live in [`../README.md`](../README.md); this folder is for the topics that need
more room than a README table.

| Document | What it covers |
|---|---|
| [`schema.md`](schema.md) | The database schema: table groups, which lesson fills which, migration rules |

Add a row when you add a document, and a `Before answering` row in
[`../CLAUDE.md`](../CLAUDE.md) if an agent should read it before working on that topic.
